# 底层熟悉路径：通俗设计说明

## 一句话概括

让模型遇到熟悉的字、词组和上下文时，不再每一层都重新寻找该用哪些参数，
而是快速得到一个“计算地址”，直接运行对应的小块 MLP；遇到陌生或不确定的
内容，再回到完整的动态判断。

它追求的不是把所有参数压得更小，而是减少运行时需要寻找和执行的范围。

## 仓库比喻

可以把模型看成一个仓库。

普通动态模型每次都要：

```text
输入问题 → 逐层查看货架 → 判断需要哪些货物 → 取货 → 计算
```

熟悉路径模型会在训练中学会货架地址：

```text
输入问题 → 得到地址37 → 直接去对应货架 → 只取需要的货物
```

量化只是让每件货物更小；直接地址则减少需要查看和搬运的货物数量。两者可以
同时使用，但它们解决的不是同一个问题。

## 四级熟悉系统

### 第一级：Token 反射

模型为常见 Token 学习一个小型地址表。看到 Token 后，通过一次查表立即激活
几个可能的 MLP 计算套餐。

```text
Token → 地址表 → 每层 MLP 的候选套餐
```

这里得到的不是固定答案，只是“接下来可能应该使用哪些计算单元”。

### 第二级：短语熟悉

单个字或 Token 可能有歧义，因此同时观察前一个 Token，把二元短语通过哈希表
映射到地址。

```text
前一个Token + 当前Token → 二元短语地址
```

Token 地址和短语地址相加，使固定搭配、常见词片段和局部语法更容易形成稳定
捷径。以后还可以扩展到三元短语。

### 第三级：上下文熟悉

整段文本的初始高维状态生成一个整网地址，对底层字词产生的候选进行修正。

```text
整段上下文 → 小型地址头 → 四层 MLP 的计算计划
```

这相当于看到熟悉词语后，再根据当前语境确认含义。

### 第四级：动态慢路径

如果地址置信度不足，模型不强行使用捷径，而是回到原来的逐层动态路由。

```text
高置信、熟悉 → 直接地址
低置信、陌生 → 逐层动态判断
```

这是系统的纠错机制。

## MLP 最小计算单元

传统 MLP 的隐藏宽度是 512。当前细粒度模型把它拆成八个连续的 64 通道原子。

```text
512通道 MLP = 8个 × 64通道原子
```

模型可以选择四种 GPU 友好的套餐：

```text
1个原子 = 64通道
2个原子 = 128通道
4个原子 = 256通道
8个原子 = 512通道
```

每个 Token 在每一层都可以选择不同的套餐。例如：

```text
Token A：第1层64，第2层128，第3层64，第4层256
Token B：第1层128，第2层128，第3层512，第4层128
```

理论上还可以让模型任意选择八个原子中的组合，但组合数量过多会让 GPU 批次
变碎。当前先限制为少量固定套餐，使相同套餐的 Token 能集中执行矩阵乘法。

最小单元没有细到单个神经元或单个参数，因为那会产生大量索引、内存访问和小
kernel 启动开销。这里的“最小”指 GPU 仍能高效执行的最小矩阵块。

## 训练时如何形成熟路

训练成本可以较高，因为目标是降低长期运行成本。

训练过程可以理解为：

1. 原始动态路由器先自由探索，为不同输入选择 MLP 套餐。
2. 记录哪些 Token、短语和上下文经常走同一条路径。
3. Token/短语地址表学习模仿这些路径。
4. 地址路径自己参与语言任务训练，而不只是机械复制旧路由。
5. 主模型和地址头共同调整，让常见内容逐渐聚集到少数稳定路径。
6. 根据实际访问次数、稳定性和置信度决定哪些路径升级为热路径。

熟悉程度可以通俗写成：

```text
熟悉度 = 出现频率 × 路由稳定性 × 地址置信度
```

只有高频且稳定的模式才适合直接执行。高频但含义很多的词，只用于快速激活候选，
仍需要上下文修正。

## 运行时如何快速反应

当前完整流程是：

```text
输入Token
   ↓
Token表 + 二元短语表快速查地址
   ↓
得到每个Token、每层MLP的套餐编号
   ↓
相同套餐的Token在GPU上分桶
   ↓
只运行64/128/256/512中被选择的矩阵块
   ↓
高频整段路径还可以直接复用CUDA Graph
   ↓
不确定内容回退动态路由
```

快速反应不是直接背出答案，而是更快找到应该使用的计算位置。

## 当前实验结果

### 上下文级熟悉

- 高频前 4 条整网路径覆盖 90.0% 的请求。
- 前 8 条覆盖 97.7%。
- 置信阈值 0.60 时，76.6% 的请求可以走直接捷径。
- 地址投影约 0.152 ms，四层动态路由约 2.776 ms。
- 动态路径 PPL 为 7.416，全部直接地址 PPL 为 7.474。

### Token/MLP 级熟悉

- 每个 Token、每层 MLP 都可以独立选择套餐。
- 52.6% 的动态 Token 选择了最小 64 通道单元。
- 完整四层 Token 地址准确率为 82.5%。
- 置信阈值 0.70 时，76.7% 的 Token 可以走捷径。
- 被接受 Token 中，91.2% 的地址与动态路由完全一致。
- Token 和二元短语查表约 0.202 ms，四层动态路由约 2.606 ms，检索约快 12.9 倍。
- 完整模型从 21.942 ms 降到 16.867 ms，端到端加速 1.301 倍。
- 动态 PPL 为 8.383；熟悉捷径加动态回退 PPL 为 8.381，没有观察到质量损失。

## 目前还没有解决的部分

1. 逐 Token 路径变化频繁，不能简单为每一种完整组合都建立 CUDA Graph。
2. 同步拆分热请求和冷请求会产生很小的 GPU 批次，可能抵消捷径收益。
3. 需要跨请求积累相同地址的 Token，再组成连续的大批次。
4. 当前使用标准 PyTorch 索引和 GEMM；融合 CUDA/Triton kernel 仍有提升空间。
5. Shakespeare 实验使用字符级 Token，更大模型还需要验证真正的词片段和语义模式。

## 下一步方向

最值得继续验证的是一个持续运行的地址队列：

```text
请求到达
  ↓
Token/短语地址立即生成
  ↓
进入对应MLP原子队列
  ↓
队列达到GPU友好批量后一起执行
  ↓
超时或低置信请求进入动态慢路径
```

这样才能把约 12.9 倍的检索加速和约 60% 的 MLP 计算节省，更充分地转化为完整
模型的端到端速度。
